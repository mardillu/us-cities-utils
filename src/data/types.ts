export type CuCity = {
    zip: string;
    city: string;
    state: string;
    stateAbbr: string;
    county: string;
    countCode: string;
    latitude: string;
    longitude: string;
};

export type CuState = {
    nameAbbr: string;
    name: string;
}